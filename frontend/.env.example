# 后端 API 地址。
# 本地开发可填 http://127.0.0.1:8000。
# 线上推荐留空或填 /，让浏览器请求同源的 /api 和 /health，由 Nginx 反代到 FastAPI。
# 不要填 0.0.0.0；它只能用于后端监听，不能作为浏览器访问地址。
VITE_API_BASE_URL=

# 高德 Web 端 JS API Key。真实值只写入 .env.local 或 .env.production，不要提交 GitHub。
VITE_AMAP_JS_KEY=
VITE_AMAP_SECURITY_CODE=
